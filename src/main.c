/*
** EPITECH PROJECT, 2019
** Sabri Ouaked
** File description:
** main.c
*/

#include "trace.h"
#include "parsing.h"

int main(int argc, char ** argv)
{
        // // if (pars_command(ac, av) == 84)
        // //         return (84);
        // test_trace(av);
        launch(argc, argv);
        return (0);
}